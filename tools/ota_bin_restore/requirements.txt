# Pinned so QuickLZ decompression is byte-identical on every machine.
#
# pyquicklz 1.4.1 bundles the original QuickLZ 1.5.0 C source compiled at
# QLZ_COMPRESSION_LEVEL 3 / QLZ_STREAMING_BUFFER 0 -- exactly the format the
# keystone3 OTA images use. The version pin therefore pins the decompression
# behaviour itself, not just the wrapper.
#
# pyquicklz ships only an sdist (no wheels), so it is compiled from source at
# install time and needs Cython present first. Install with:
#
#     pip install Cython
#     pip install --no-build-isolation -r requirements.txt
#
# The hash pins the exact source tarball (platform-independent) for a
# reproducible, tamper-evident install.
pyquicklz==1.4.1 --hash=sha256:4d09ea26f6f0ff41249c912b75a1d70ee4110852449d6bfefe4d5831829fc226
